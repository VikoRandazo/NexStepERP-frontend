export type BtnActionsText =
  | `Edit Product`
  | `Create Product`
  | `Update Product`
  | `Cancel`
  | `Search`
  | `Action`;

export enum BtnActionsTextEnum {
  EDIT = `Edit Product`,
  CREATE = `Create Product`,
  UPDATE = `Update Product`,
  CANCEL = `Cancel`,
  SEARCH = `Search`,
  Action = `Action`,
}
